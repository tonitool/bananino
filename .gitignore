node_modules/
build/
release/

# The optimised models are generated from the .source.glb files by
# `npm run optimise-model`. Keeping both would double the repository size for no benefit.
assets/characters/*.glb
!assets/characters/*.source.glb
